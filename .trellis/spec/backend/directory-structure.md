# Backend Directory Structure

There is no backend directory. Do not create routes, controllers, services, or
API modules unless a task explicitly introduces a backend.

`tools/serve_utf8.py` is a development utility, not an application server.
